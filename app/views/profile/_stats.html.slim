.rounded-lg.shadow-lg.bg-white.flex.flex-col.items-center
  svg viewBox = "0 0 512 512" height = "120" width = "120" fill = "#4D4B48"
    path d = svg_path
  .text-gray-500.text-lg.font-bold.mb-1
    = title
  .w-10/12.mx-3.border.rounded-xl.border-gray-300
  .flex.justify-around.w-full.py-8
    - items.each do |item|
      .items-center.justify-center.flex.flex-col
        .text-center.text-lg.font-medium.mb-2
          = item[:title]
        .text-5xl.font-black class = (item[:number] == 0 ? "text-gray-500" : (item[:number] < 0 ? "text-red-700" : "text-green-700"))
          = item[:number]
