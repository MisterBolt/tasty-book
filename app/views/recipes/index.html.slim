.px-10.mt-3
  .flex.justify-center.mt-1.mb-4
    == pagy_nav(@pagy) if @pagy.pages > 1
  - if @recipes.present?
    = render @recipes
  - else 
    p.text-center.mt-4
      = t("recipes.not_found")
  .flex.justify-center.my-4
    == pagy_nav(@pagy) if @pagy.pages > 1
